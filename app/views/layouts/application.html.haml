!!!
%html
  %head
    %title #{title} - POBox
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

  %body
    - if flash[:success]
      #success-flash= flash[:success]
    - if flash[:error]
      #error-flash= flash[:error]

    %nav
      - if logged_in?
        Hello, #{current_user.nickname} | #{link_to "Log out", logout_path}
      - else
        #{link_to_unless_current "Sign up", signup_path} | #{link_to_unless_current "Log in", login_path}

    %h1= title

    = yield
